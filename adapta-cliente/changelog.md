# Changelog — Projeto Imobiliária Janeide Xavier LTDA

> Registro de tudo que acontece no projeto, em ordem cronológica inversa (mais recente no topo).
> Formato: `- AAAA-MM-DD · [quem] · o que aconteceu`
> **Dúvidas para o consultor** entram como: `- AAAA-MM-DD · [quem] · DÚVIDA: …` — ele responde
> na próxima sincronização.

## Registro

- 2026-09-07 · [Matheus Silva] · Task F1-T10 concluída: permissões, auditoria e dados incompletos do painel (SPEC-1-002) exercitados no projeto Skip 55154 (deploys 0.0.37–0.0.49) — migrations 0010 (`role` no usuário sdr/corretor/admin/outro, `assigned_to` no lead, SDR marcado), 0011/0013–0016 (corretor de teste criado/atualizado a partir do secret `CORRETOR_TEST_CREDENTIALS`, sem exposição de valor), 0012/0017–0019 (atribuição do lead de teste ao corretor atual, com fallback por nome), hook `GET /backend/v1/painel/lead/{lead_id}` (matriz por papel: SDR/Admin veem telefone de todos; corretor só do lead atribuído; log `acesso_negado` em `lead_audit` **sem gravar o valor** — LGPD/RN-4) e sinalização de dados incompletos (CA-1-08: `incomplete`/`missing_fields` no endpoint + aviso âmbar no cartão, sem inventar dado). Teste humano aprovado por Janeide Xavier (4/4 cenários: SDR vê telefone; corretor vê do lead atribuído; corretor não vê de lead alheio; aviso de incompletude — caso de cache do navegador resolvido com hard refresh). QA verde até 0.0.49. Aprendizado AP-2026-09-07-1727 registrado (validar write com leitura de volta; migration idempotente pode aplicar sem efeito; diagnóstico bundle→endpoint→cache).
- 2026-09-04 · [Matheus Silva] · Task F1-T09 concluída: bordas da captura e fila de pendência (SPEC-1-001) exercitadas no projeto Skip 55154 (deploy 0.0.36) — 5 cenários aprovados em teste humano com assinatura pré-calculada: (1) `source_channel` inválido → 400; (2) `property_code` inexistente no catálogo → `200 pending invalid_property_code` **sem criar lead** (validação contra catálogo adicionada, CA-1-03/RN-5); (3) repetição do mesmo `capture_event_id` → `201 created` / `200 duplicate` com mesmo `lead_id` (idempotência CA-1-02); (4) opt-out → `200 rejected` sem lead (CA-1-04); (5) falha de save coberta por try/catch → evento `error` + `503 save_failed` (RN-4). QA verde. **Pendência:** `META_APP_SECRET` ficou com valor de teste conhecido — requer rotação pelo usuário antes de produção.
- 2026-09-03 · [Janeidinha] · F1-T09 implementada: webhook valida `property_code` contra catálogo (inexistente → pendência) + falha de save com evento `error`/503 (RN-4); deploy 0.0.36.
- 2026-09-03 · [Matheus Silva] · Task F1-T08 concluída: registro de pedido de visita sem reserva (SPEC-1-004) implementada no projeto Skip 55154 — migration 0009 (coleção `visit_requests` com 14 campos + seed lead fictício `lead-teste-visita-001`), hooks `POST /backend/v1/visita/registrar` e `/visita/cancelar` (auth, vínculo lead+imóvel RN-2, janela honesta RN-3, idempotência CA-1-16, auditoria RN-4, **sem reserva/slot/corretor RN-1/CA-1-14`), UI no cartão (registrar com máscara DD/MM/AAAA HH:MM, listar, cancelar com motivo). Teste humano aprovado por Matheus (4ª tentativa — após correções): dedupe (repetir mesma janela não duplica; janela diferente cria novo), máscara de digitação, `Precisa esclarecer` com janela vazia confirmado, cancelamento com motivo. QA verde até 0.0.35. Correções durante o ciclo: `created_by` com `actor.id` (email vazio no auth record — AP-2026-09-03-1150), conversão datetime→RFC3339 com -03:00 (deploy 0.0.33), dedupe com `findRecordsByFilter` ignorando cancelados (deploy 0.0.35). 
- 2026-09-07 · [Matheus Silva] · Task F1-T11 concluída: bloqueios de catálogo, mídia e envio duplicado (SPEC-1-003) exercitados no Skip 55154 — inativo, incompleto sem price, vencido, mídia insegura (2 URLs), preview válido e envio idempotente (registrado na 1ª confirmação / duplicate na 2ª) aprovados por Janeide em 7 cenários. QA verde até 0.0.56. Correções durante o ciclo: campo JSON em goja chegava como bytes (84/142/78 eram bytes do JSON; normalização `toList` + validação segura), PocketBase number vazio normaliza para 0 (convenção Fase 1 `price > 0` informado, `<= 0` incompleto), corpo da rota via `e.requestInfo().body` (400 genérico corrigido). Dívida deliberada: distinguir preço `consulte` de ausente exige revisão futura do modelo.
